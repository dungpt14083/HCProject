using UnityEngine;
using System.Collections;
using NBCore;

public class MainUIControlller : MonoBehaviour {
  bool isShow = false;
  public void ShowTestDialog() {
//    if (!isShow) {
//      GUIManager.Instance.ShowPanel(GUIManager.Instance.testDialog);
//    } else {
//      GUIManager.Instance.HidePanel(GUIManager.Instance.testDialog);
//    } // else
//    isShow = !isShow;
  } // ShowDialog ()
}
